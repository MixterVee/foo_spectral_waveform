name: Apply reverse release visual clock

on:
  push:
    paths:
      - '.github/reverse_release_visual_clock_patch.py'
      - '.github/workflows/apply-reverse-release-visual-clock.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply patch
        run: python .github/reverse_release_visual_clock_patch.py
      - name: Commit source change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add spectral_ui.cpp
          git commit -m "Bridge reverse release with forward visual clock"
          git push
