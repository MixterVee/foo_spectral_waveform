name: Fold tested Spectral patches into source

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  fold:
    if: github.event.head_commit.message == 'Fold tested Spectral patches into source'
    runs-on: windows-2022
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply exact tested source transforms
        shell: powershell
        run: |
          .\tools\apply_mute_grab_patch.ps1
          .\tools\remove_scratch_debug_overlay.ps1

      - name: Remove temporary patch scaffolding
        shell: powershell
        run: |
          $workflow = '.github/workflows/build-native.yml'
          $text = [System.IO.File]::ReadAllText($workflow) -replace "`r`n", "`n"
          $blocks = @(
@'
      - name: Apply mute-on-grab patch
        shell: pwsh
        run: .\tools\apply_mute_grab_patch.ps1

'@,
@'
      - name: Remove scratch debug overlay
        shell: pwsh
        run: .\tools\remove_scratch_debug_overlay.ps1

'@
          )
          foreach ($block in $blocks) {
            if (-not $text.Contains($block)) { throw "Expected build patch step not found." }
            $text = $text.Replace($block, '')
          }
          [System.IO.File]::WriteAllText($workflow, $text, [System.Text.UTF8Encoding]::new($false))

          Remove-Item tools\apply_mute_grab_patch.ps1 -Force
          Remove-Item tools\remove_scratch_debug_overlay.ps1 -Force
          Remove-Item .github\workflows\fold-tested-patches.yml -Force

      - name: Commit clean source baseline
        shell: powershell
        run: |
          git config user.name "MixterVee"
          git config user.email "mixtervandal@gmail.com"
          git add spectral_ui.cpp .github/workflows/build-native.yml tools .github/workflows/fold-tested-patches.yml
          git commit -m "Make tested Spectral baseline permanent"
          git push origin HEAD:main
